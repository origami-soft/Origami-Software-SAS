<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>

        <record model="ir.ui.view" id="account_move_invoice_currency_rate_form">
            <field name="name">Account move invoice currency rate form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@id='header_right_group']" position="inside">
                    <field name="need_rate" invisible="1"/>
                    <field
                        name="currency_rate" string="Cotización factura" groups="base.group_multi_currency"
                        invisible="not need_rate or move_type not in ['in_invoice', 'in_refund', 'out_invoice', 'out_refund']"
                        readonly="state != 'draft'"
                    />
                    <field
                        name="current_currency_rate"
                        groups="base.group_multi_currency"
                        invisible="not need_rate or state != 'draft' or move_type not in ['in_invoice', 'in_refund', 'out_invoice', 'out_refund']"
                    />
                    <div class="alert alert-warning" role="alert" colspan="2" invisible="not need_rate or state != 'draft' or move_type not in ['in_invoice', 'in_refund', 'out_invoice', 'out_refund']">
                        Si carga una cotización en <strong>cotización factura</strong>, se utilizará la misma para hacer las conversiones. De lo contrario se utilizará la <strong>cotización actual</strong>.
                    </div>
                </xpath>
            </field>
        </record>

    </data>
</odoo>

<!-- vim:expandtab:smartindent:tabstop=4:softtabstop=4:shiftwidth=4-->