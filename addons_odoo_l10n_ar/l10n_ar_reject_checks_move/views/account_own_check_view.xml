<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record model="ir.ui.view" id="own_check_reject_move_form">
        <field name="name">Own check reject move form</field>
        <field name="model">account.own.check</field>
        <field name="inherit_id" ref="l10n_reject_checks.own_check_reject_form"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="reject_move_id" invisible="not reject_move_id"/>
            </field>
            <field name="payment_date" position="after">
                <field name="reject_date" invisible="not reject_date"/>
            </field>
            <button name="reject_check" position="attributes">
                <attribute name="invisible">1</attribute>
            </button>
            <button name="revert_reject" position="attributes">
                <attribute name="confirm">Se eliminará el asiento de rechazo.</attribute>
            </button>
            <header position="inside">
                <button name="get_reject_check_wizard" string="Rechazar" type="object"
                        invisible="state not in ['handed', 'reconciled']"
                        groups="l10n_treasury.group_account_treasury_manager"/>
            </header>
        </field>
    </record>

</odoo>

<!-- vim:expandtab:smartindent:tabstop=4:softtabstop=4:shiftwidth=4-->
